<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Settings - Llm Tester" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= yield :head %>
    <link rel="icon" href="/icon.png" type="image/png">
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <body>
    <%= render "shared/navbar" %>
    <main class="container pb-5">
      <%= render "shared/flash" %>
      <div class="row">
        <div class="col-md-3">
          <%= render "settings/nav" %>
        </div>
        <div class="col-md-9">
          <%= yield %>
        </div>
      </div>
    </main>
  </body>
</html>
