# This file is generated from lib/database_config.rb.
#
# By default the app uses SQLite (databases live under storage/). To run on
# PostgreSQL instead, set DATABASE_ADAPTER=postgresql and the DATABASE_* vars
# in your .env file. See lib/database_config.rb for the full list of variables.
<% require Rails.root.join("lib/database_config").to_s %>
<%= DatabaseConfig.rendered %>
